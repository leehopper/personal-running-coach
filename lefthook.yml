pre-commit:
  parallel: true
  commands:
    reject-docs-specs:
      glob: "docs/specs/**"
      run: |
        echo "ERROR: docs/specs/ is gitignored — do not stage these files."
        echo "Offending files: {staged_files}"
        exit 1
    dotnet-format:
      root: "backend/"
      glob: "*.cs"
      run: dotnet format RunCoach.slnx --include {staged_files} --no-restore
      stage_fixed: true
    eval-cache-ttl-fix:
      glob: "**/eval-cache/**/entry.json"
      run: bash backend/tests/scripts/fix-eval-cache-ttl.sh
      stage_fixed: true
    eval-cache-dedup:
      glob: "**/eval-cache/**"
      run: bash backend/tests/scripts/clean-eval-cache-duplicates.sh
    eslint:
      root: "frontend/"
      glob: "*.{ts,tsx}"
      run: npx eslint --fix {staged_files}
      stage_fixed: true
    prettier:
      root: "frontend/"
      glob: "*.{ts,tsx,css,json}"
      run: npx prettier --write {staged_files}
      stage_fixed: true

commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}

pre-push:
  parallel: true
  commands:
    dotnet-test:
      root: "backend/"
      glob: "*.cs"
      run: EVAL_CACHE_MODE=Replay dotnet test RunCoach.slnx --no-restore --filter "Category!=Integration"
    eval-cache-replay:
      glob: "*.cs"
      run: bash backend/tests/scripts/prepush-eval-check.sh
    eval-cache-ttl-check:
      glob: "**/eval-cache/**/entry.json"
      run: bash backend/tests/scripts/check-eval-cache-ttl.sh
    typescript-check:
      root: "frontend/"
      glob: "*.{ts,tsx}"
      run: npx tsc --noEmit
