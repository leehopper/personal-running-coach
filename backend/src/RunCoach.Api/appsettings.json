{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "_ConnectionStrings_comment": "Connection strings with credentials are loaded from user-secrets (local dev: `dotnet user-secrets set \"ConnectionStrings:runcoach\" \"Host=...;Password=...\"` --project backend/src/RunCoach.Api) or environment variables (docker compose / CI). Never store credentials in committed config files.",
  "ConnectionStrings": {
    "Redis": "localhost:6379"
  },
  "Prompts": {
    "BasePath": "Prompts",
    "ActiveVersions": {
      "coaching-system": "v1"
    }
  }
}
