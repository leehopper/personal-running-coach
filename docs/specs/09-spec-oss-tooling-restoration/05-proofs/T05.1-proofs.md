# T05.1 Proof Summary - SonarQube Cloud Restore

## Task
Create `.github/workflows/sonarqube.yml` and `frontend/sonar-project.properties` for SonarQube Cloud CI integration (Unit 5 of OSS Tooling Restoration).

## Files Created
1. `.github/workflows/sonarqube.yml` - Two-job workflow (backend + frontend analysis)
2. `frontend/sonar-project.properties` - Frontend SonarQube Cloud configuration

## Proof Artifacts

| # | Type | File | Status |
|---|------|------|--------|
| 1 | file | T05.1-01-file-workflow.txt | PASS |
| 2 | file | T05.1-02-file-sonar-properties.txt | PASS |
| 3 | cli | T05.1-03-cli-build.txt | PASS |
| 4 | cli | T05.1-04-cli-tests.txt | PASS |

## Key Verification Points

- Workflow defines two jobs: `backend-analysis` and `frontend-analysis`
- All 5 `uses:` lines are SHA-pinned with `# vX.Y.Z` comments
- Both jobs have `continue-on-error: true` for bake-week soft launch
- Backend uses `dotnet-sonarscanner` v11.2.1 (auto-provisions JRE, no setup-java)
- Frontend uses `SonarSource/sonarqube-scan-action@v7.0.0`
- `sonar.cs.opencover.reportsPaths` set to Unit 1's output path
- `sonar.cs.cobertura.reportsPaths` is NOT set (property does not exist in SonarQube Cloud)
- `sonar.javascript.lcov.reportPaths` set for frontend coverage
- Backend exclusions include eval-cache, generated files, Prompts (coaching IP no-touch)
- Build-time analyzers (SonarAnalyzer.CSharp, eslint-plugin-sonarjs) untouched
- `dotnet build` passes with 0 warnings, 0 errors
- All 419 backend tests pass

## Notes
- Backend Sonar properties passed via `/d:` flags per R-038 guidance (dotnet-sonarscanner does not read sonar-project.properties)
- Uses single `SONAR_TOKEN` secret per spec (not separate per-project tokens)
- checkout and setup-dotnet SHAs match existing ci.yml for consistency
- setup-node SHA matches existing ci.yml for consistency
- URL-based proofs (SonarQube Cloud dashboard, PR decoration) require GitHub CI execution and are deferred to post-merge validation
