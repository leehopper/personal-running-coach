Proof Artifact: Directory.Build.props Configuration
Type: file
Purpose: Demonstrates Coverlet dual-output configuration

FILE: backend/Directory.Build.props
CHANGES MADE:
  - Added: <CoverletOutputFormat>cobertura,opencover</CoverletOutputFormat>
  - Added: <CoverletOutput>../../TestResults/</CoverletOutput>

VERIFICATION:
Current file contents (relevant section):
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <LangVersion>14</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <AnalysisLevel>latest-recommended</AnalysisLevel>
    <EnforceCodeStyleInBuild>true</EnforceCodeStyleInBuild>
    <CoverletOutputFormat>cobertura,opencover</CoverletOutputFormat>
    <CoverletOutput>../../TestResults/</CoverletOutput>
  </PropertyGroup>

IMPACT:
When dotnet test is run with /p:CollectCoverage=true, Coverlet will:
1. Generate a .cobertura.xml file in backend/TestResults/ for Codecov consumption
2. Generate a .opencover.xml file in the same directory for SonarQube Cloud consumption in Unit 5
3. Both formats contain identical coverage data in their respective XML schemas

Expected output paths (when tests complete successfully):
- backend/TestResults/coverage.cobertura.xml
- backend/TestResults/coverage.opencover.xml

Status: PASS - Configuration is syntactically correct and matches Coverlet v8.0.1 expectations
Timestamp: 2026-04-15T17:35:00Z
