Proof Artifact: CI Workflow Configuration
Type: file
Purpose: Demonstrates removal of dotnet-quality: preview and Coverlet property consolidation

FILE: .github/workflows/ci.yml
CHANGES MADE:

1. REMOVED (line 56):
   - Previous: dotnet-quality: 'preview'
   - Reason: .NET 10 reached GA status; preview quality channel no longer necessary

2. SIMPLIFIED Test step (line 61):
   - Previous: dotnet test backend/RunCoach.slnx --no-build /p:CollectCoverage=true /p:CoverletOutputFormat=cobertura /p:CoverletOutput=../../TestResults/
   - Current:  dotnet test backend/RunCoach.slnx --no-build /p:CollectCoverage=true
   - Reason: CoverletOutputFormat and CoverletOutput now declared globally in Directory.Build.props

VERIFICATION:
Git diff output:
---
 - uses: actions/setup-dotnet@c2fa09f4bde5ebb9d1777cf28262a3eb3db3ced7  # v5.2.0
   with:
     dotnet-version: '10.0.x'
-    dotnet-quality: 'preview'
 - name: Restore
---
 - name: Test
-  run: dotnet test backend/RunCoach.slnx --no-build /p:CollectCoverage=true /p:CoverletOutputFormat=cobertura /p:CoverletOutput=../../TestResults/
+  run: dotnet test backend/RunCoach.slnx --no-build /p:CollectCoverage=true

IMPACT:
1. CI workflow uses default (GA) .NET 10 quality channel
2. Coverlet configuration is now centralized in Directory.Build.props (single source of truth)
3. Properties can be overridden per-command if needed, but default is globally consistent
4. Both Cobertura and OpenCover formats will be generated per Directory.Build.props settings

Other workflows (backend, frontend, security, gate jobs) remain unchanged in behavior and pass/fail outcomes.

Status: PASS - Configuration is minimal and non-disruptive
Timestamp: 2026-04-15T17:35:00Z
