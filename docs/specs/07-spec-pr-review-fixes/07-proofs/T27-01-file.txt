Type: file
Description: Verify duplicate XML doc sentences removed from ClaudeCoachingLlm constructors
File: backend/src/RunCoach.Api/Modules/Coaching/ClaudeCoachingLlm.cs
Expected: No duplicate "Initializes a new instance" sentences in constructor XML docs
Timestamp: 2026-03-25T20:35:00Z

Note: SA1642 (StyleCop) requires constructor summaries to start with
"Initializes a new instance of the <see cref="ClassName"/> class."
and dotnet format auto-fixes violations. The fix merges the duplicate
lines into a single sentence that satisfies SA1642.

--- Before (first constructor) ---
    /// <summary>
    /// Initializes a new instance of the <see cref="ClaudeCoachingLlm"/> class.
    /// Initializes a new instance using dependency-injected settings and logger.
    /// Creates the <see cref="AnthropicClient"/> from configuration.
    /// </summary>

--- After (first constructor) ---
    /// <summary>
    /// Initializes a new instance of the <see cref="ClaudeCoachingLlm"/> class
    /// using dependency-injected settings and logger.
    /// Creates the <see cref="AnthropicClient"/> from configuration.
    /// </summary>

--- Before (second constructor) ---
    /// <summary>
    /// Initializes a new instance of the <see cref="ClaudeCoachingLlm"/> class.
    /// Initializes a new instance with an externally provided client.
    /// Used for testing with a mock/substitute client.
    /// </summary>

--- After (second constructor) ---
    /// <summary>
    /// Initializes a new instance of the <see cref="ClaudeCoachingLlm"/> class
    /// with an externally provided client for testing with a mock/substitute.
    /// </summary>

Verification: dotnet format reports 0 files changed (SA1642 satisfied).

Status: PASS
