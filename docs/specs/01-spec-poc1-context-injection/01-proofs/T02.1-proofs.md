# T02.1 Proof Summary: Create coaching system prompt and context injection YAML files

## Task

Create `coaching-v1.yaml` and `context-injection-v1.yaml` in `backend/src/RunCoach.Api/Prompts/`.

## Proof Artifacts

| # | Type | File | Status |
|---|------|------|--------|
| 1 | file | T02.1-01-file.txt | PASS |
| 2 | file | T02.1-02-file.txt | PASS |
| 3 | cli  | T02.1-03-cli.txt  | PASS |

## Summary

### coaching-v1.yaml (334 lines)

- Coaching persona with warmth/directness 80/20 calibration, OARS communication framework, Elicit-Provide-Elicit information delivery, modified GROW conversation structure
- 9 "always do" rules and 14 "never do" rules from coaching-persona.md
- 8 reframing vocabulary patterns (data-not-failure, not-yet, etc.)
- AI-specific guidelines (no emotional performance, proactive check-ins)
- Safety section: medical boundary, injury protocol, crisis response (988 + 741741), nutrition boundary, overtraining detection
- 7 keyword trigger categories per DEC-019 and DEC-030
- 3-tier sensitive disclosure escalation (green/amber/red)
- Output format: JSON plan data in code fence + natural language coaching notes
- Schema definitions for MacroPlan, MesoWeek, MicroWorkout matching C# data model types
- JSON example demonstrating expected output structure
- Context injection template markers (11 placeholders) with positional system prompt template
- Deterministic guardrails (pace authority, 10% volume progression cap, deload rules)
- Model configuration: parameterized model ID, temperature 0.3, max_tokens 4096

### context-injection-v1.yaml (177 lines)

- Total token budget: 15,000 tokens
- 3-section positional layout matching U-curve research:
  - START (~6,300 tokens): system prompt, user profile, goal state, fitness estimate, training paces, plan state, metrics
  - MIDDLE (~4,700 tokens): training history (Layer 1 per-workout + Layer 2 weekly summary), relevant plan context
  - END (~4,000 tokens): conversation history (max 10 turns), current user message
- Token estimation: character_ratio method (chars/4, 10% safety margin)
- 5-step overflow reduction cascade for budget enforcement
- Summarization layer definitions for training history (Layer 1 and Layer 2)
- Format templates for each component matching C# model field names

### Validation

Both files validated as syntactically correct YAML using Ruby's YAML.safe_load parser (standards-compliant YAML 1.1). No tabs detected. Both files are designed to be parseable by YamlDotNet.

## All proofs passing: 3/3
