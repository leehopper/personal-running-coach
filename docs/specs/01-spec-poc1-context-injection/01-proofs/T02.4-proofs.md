# T02.4 Proof Summary: Create RunCoach.Poc1.Console CLI App

## Task
Create a .NET console application for interactive prompt exploration and plan generation.

## Proof Artifacts

| # | Type | File | Status | Description |
|---|------|------|--------|-------------|
| 1 | file | T02.4-01-file.txt | PASS | All project files created (csproj, Program.cs, AssemblyMarker.cs) and solution updated |
| 2 | cli  | T02.4-02-cli.txt  | PASS | CLI argument handling: missing profile (exit 1), unknown profile (exit 1), valid profile with context assembly |
| 3 | cli  | T02.4-03-cli.txt  | PASS | Solution builds with 0 warnings/errors, all 164 existing tests pass |

## Implementation Summary

- Created `backend/src/RunCoach.Poc1.Console/RunCoach.Poc1.Console.csproj` (console app referencing RunCoach.Api)
- Created `backend/src/RunCoach.Poc1.Console/Program.cs` with CLI argument parsing, DI setup, context assembly, and LLM calling
- Created `backend/src/RunCoach.Poc1.Console/AssemblyMarker.cs` for user-secrets configuration binding
- Updated `backend/RunCoach.slnx` to include the new project
- Updated `backend/Directory.Packages.props` to add Microsoft.Extensions.Hosting package version

## Key Behaviors Verified

- `--profile <name>` required argument with validation against 5 valid profiles
- `--prompt-version <version>` optional argument defaulting to "v1"
- Unknown profile name produces non-zero exit code with stderr error message
- Missing `--profile` argument produces non-zero exit code with stderr error message
- Missing API key produces non-zero exit code with stderr error message (no partial request sent)
- Context assembly produces correct structured prompt from test profile data
- LLM call is attempted with assembled system prompt and user message
- JSON extraction from LLM response code blocks
