Type: cli
Description: Both YAML files parse successfully with a standards-compliant YAML parser
Command: ruby -ryaml -e "['coaching-v1.yaml', 'context-injection-v1.yaml'].each { |f| path = 'backend/src/RunCoach.Api/Prompts/' + f; data = YAML.safe_load(File.read(path)); puts 'PASS: ' + f + ' — top-level keys: ' + data.keys.join(', ') }; puts 'Both YAML files are syntactically valid.'"
Expected: Both files parse without error
Timestamp: 2026-03-21T00:00:00Z

--- Output ---
PASS: coaching-v1.yaml — top-level keys: version, model, persona, safety, output_format, context_injection
PASS: context-injection-v1.yaml — top-level keys: version, total_token_budget, sections, token_estimation, overflow_strategy
Both YAML files are syntactically valid.

Status: PASS
