# T03.2 Proof Summary: Implement plan generation eval tests for all 5 profiles

## Task
Create xUnit integration tests that generate training plans for all 5 profiles and assert hard safety/constraint requirements.

## Proof Artifacts

| # | Type | File | Status |
|---|------|------|--------|
| 1 | test | T03.2-01-test.txt | PASS |
| 2 | cli  | T03.2-02-cli.txt  | PASS |
| 3 | file | T03.2-03-file.txt | PASS |

## Summary

- **PlanGenerationEvalTests** created at `tests/RunCoach.Api.Tests/Modules/Coaching/Eval/PlanGenerationEvalTests.cs`
  - Sealed class inheriting from EvalTestBase, tagged with `[Trait("Category", "Eval")]`
  - 5 eval test methods, one per profile, each calling the live Claude API and asserting hard constraints
- **Profile-specific assertions implemented:**
  - **Sarah (Beginner):** Volume limited to 10% increase (max 16.5km/week), no speed work, 2+ rest days
  - **Lee (Intermediate):** Paces dynamically derived from VDOT computation (not hardcoded), all paces within computed zone ranges with 15s tolerance
  - **Maria (Goalless):** Weekly volume within +/-10% of 55km, workout variety requirement
  - **James (Injured):** 20-minute max duration, easy-only pace, 4+ week gradual ramp-up, injury acknowledgment and medical deference
  - **Priya (Constrained):** Exactly 4 run days / 3 rest days, no early morning scheduling references
- **Dual assertion strategy:** Both structured JSON parsing (MesoWeek, MicroWorkouts, MacroPlan) and text-based regex scanning of response
- **Lee's pace assertions are fully dynamic:** Paces derive from the profile's VDOT computation via PaceCalculator. If Lee's race time changes, assertions automatically update.
- Each test writes structured output to `poc1-eval-results/{profile}-plan.json` for manual review
- Build: 0 warnings, 0 errors
- Tests: 184 existing (non-eval) tests pass, 5 new eval tests correctly excluded from normal CI
