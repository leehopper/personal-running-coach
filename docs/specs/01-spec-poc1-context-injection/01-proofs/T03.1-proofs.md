# T03.1 Proof Summary: Create eval test infrastructure and base class

## Task
Build shared eval test infrastructure used by both plan generation and safety boundary test classes.

## Proof Artifacts

| # | Type | File | Status |
|---|------|------|--------|
| 1 | test | T03.1-01-test.txt | PASS |
| 2 | cli  | T03.1-02-cli.txt  | PASS |
| 3 | file | T03.1-03-file.txt | PASS |

## Summary

- **EvalTestBase** abstract class created at `tests/RunCoach.Api.Tests/Modules/Coaching/Eval/EvalTestBase.cs`
  - Reads Anthropic API key from user-secrets (UserSecretsId: `runcoach-api-tests`) or `ANTHROPIC_API_KEY` env var
  - Creates real `ClaudeCoachingLlm` instance with production `ContextAssembler`
  - Provides profile loading, context assembly, LLM calling, and result writing helpers
  - JSON parsing helpers for MacroPlan, MesoWeek, MicroWorkout extraction
  - All public static helpers can be used without API key (for unit testing)
  - Implements `IDisposable` for proper LLM client cleanup
- **EvalTestBaseTests** with 20 unit tests covering all static helper methods
- Test project updated with `UserSecretsId` and configuration packages
- Central `Directory.Packages.props` updated with configuration package versions
- `.gitignore` updated to exclude `poc1-eval-results/` directory
- Build: 0 warnings, 0 errors
- Tests: 184 total (164 existing + 20 new), all passing
