Type: file
Description: context-injection-v1.yaml exists with positional layout template
Command: ls -la backend/src/RunCoach.Api/Prompts/context-injection-v1.yaml && ruby -ryaml -e "data = YAML.safe_load(File.read('backend/src/RunCoach.Api/Prompts/context-injection-v1.yaml')); puts data.keys.join(', ')"
Expected: File exists and contains keys: version, total_token_budget, sections, token_estimation, overflow_strategy
Timestamp: 2026-03-21T00:00:00Z

--- Output ---
File: backend/src/RunCoach.Api/Prompts/context-injection-v1.yaml
Exists: YES
Size: 177 lines

Top-level YAML keys: version, total_token_budget, sections, token_estimation, overflow_strategy

Total token budget: 15000

Sections (3 positional zones):
  1. START (stable prefix, ~6300 tokens, cacheable):
     - system_prompt (2800 tokens, required)
     - user_profile (800 tokens, required)
     - goal_state (300 tokens, required)
     - fitness_estimate (300 tokens, required)
     - training_paces (400 tokens, required)
     - current_plan_state (500 tokens, optional)
     - computed_metrics (200 tokens, optional)

  2. MIDDLE (variable, ~4700 tokens, not cacheable):
     - training_history (3500 tokens, optional, Layer 1 + Layer 2 summarization)
     - relevant_plan_context (1200 tokens, optional)

  3. END (conversational, ~4000 tokens, not cacheable):
     - conversation_history (3200 tokens, optional, max 10 turns, oldest-first truncation)
     - current_user_message (800 tokens, required)

Token estimation method: character_ratio (chars/4, 10% safety margin)

Overflow strategy: 5-step reduction cascade
  1. Reduce training_history to Layer 2 only
  2. Truncate oldest conversation turns
  3. Remove relevant_plan_context
  4. Reduce training_history to 2 weeks
  5. Truncate conversation to 3 turns

Status: PASS
