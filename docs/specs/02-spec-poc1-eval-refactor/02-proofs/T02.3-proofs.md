# T02.3 Proof Summary

## Task: Update console app and verify end-to-end YAML prompt loading

### Fix Applied
Set `AppContext.BaseDirectory` as content root and config base path in `Program.BuildHost()` so that `appsettings.json` and `Prompts/*.yaml` files are found regardless of the current working directory.

### Proof Artifacts

| # | Type | Status | File |
|---|------|--------|------|
| 1 | cli  | PASS   | T02.3-01-cli.txt |
| 2 | cli  | PASS   | T02.3-02-cli.txt |

### Notes
- Full end-to-end CLI verification (`dotnet run --project ... --profile lee`) requires a live API key and is deferred to T04 eval suite
- Build and all 343 unit tests pass with the content root fix
