# T01.1 Proof Summary: Audit and pin all GitHub Action versions in ci.yml

## Task

Audit every `uses:` reference in `.github/workflows/ci.yml`. Fix `actions/setup-node@v6` to `@v4`. Verify all other action versions are pinned to latest known stable major versions.

## Changes Made

1. **Fixed**: `actions/setup-node@v6` -> `actions/setup-node@v4` (line 76)
   - v6 tag does not exist; v4 is the latest stable major version
   - Added inline comment documenting the verification date

2. **Verified (no change needed)**:
   - `actions/checkout@v6` -- Dependabot-verified bump from v4 (PR #2, 2026-03-19)
   - `actions/setup-dotnet@v5` -- Dependabot-verified bump from v4 (PR #4, 2026-03-19)
   - `codecov/codecov-action@v5` -- Dependabot-verified bump from v4 (PR #3, 2026-03-19)
   - `dorny/paths-filter@v4` -- Dependabot-verified bump from v3 (PR #6, 2026-03-19)

3. **Annotated (no version change)**: `aquasecurity/trivy-action@master`
   - This action does not publish stable major version tags (uses 0.x.x semver)
   - Added TODO comments on all 3 occurrences to pin when a stable major version tag becomes available

## Proof Artifacts

| # | File | Type | Status |
|---|------|------|--------|
| 1 | T01.1-01-cli.txt | cli | PASS |
| 2 | T01.1-02-file.txt | file | PASS |

## Verification Method

- Git history (`git log --oneline -- .github/workflows/ci.yml`) confirmed all Dependabot bumps
- `grep -n 'uses:' .github/workflows/ci.yml` confirms no `@v6` for setup-node
- `grep -c 'setup-node@v6' .github/workflows/ci.yml` returns 0

## Result: PASS
