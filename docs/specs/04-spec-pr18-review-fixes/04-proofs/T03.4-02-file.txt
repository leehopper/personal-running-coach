Type: file
Description: Verify upper-bound check added to CheckPaceRanges in PlanConstraintEvaluator
Expected: New guard checks TargetPaceFastSecPerKm > easyMaxSec and adds violation
Timestamp: 2026-03-22T15:50:00Z
Status: PASS

Diff (PlanConstraintEvaluator.cs):
--- a/backend/tests/RunCoach.Api.Tests/Modules/Coaching/Eval/PlanConstraintEvaluator.cs
+++ b/backend/tests/RunCoach.Api.Tests/Modules/Coaching/Eval/PlanConstraintEvaluator.cs
@@ -168,6 +168,14 @@ public sealed class PlanConstraintEvaluator : IEvaluator
             {
                 violations.Add(...faster than rep floor...);
             }
+
+            // Upper bound: fast pace should not exceed easy pace max (would mean the
+            // "fast" workout is actually easy or slower).
+            var easyMaxSec = (int)easyRange.MaxPerKm.TotalSeconds;
+            if (workout.TargetPaceFastSecPerKm > easyMaxSec)
+            {
+                violations.Add($"Workout '{workout.Title}' fast pace {workout.TargetPaceFastSecPerKm}s/km slower than easy max ({easyMaxSec}s/km).");
+            }
         }
     }

New test (PlanConstraintEvaluatorTests.cs):
  Evaluate_FastPaceSlowerThanEasyMax_ReturnsViolation
  - Sets fast pace to 400s/km with easy max 360s/km
  - Asserts single violation containing "slower than easy max"
