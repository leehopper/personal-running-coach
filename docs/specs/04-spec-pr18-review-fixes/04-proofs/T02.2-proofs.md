# T02.2 Proof Summary

## Task
Remove sync Assemble path from ContextAssembler and IContextAssembler

## Changes Made
- **IContextAssembler.cs**: Removed sync `Assemble(ContextAssemblerInput)` method declaration; updated doc comments to remove backward compat references
- **ContextAssembler.cs**: Removed `SystemPromptText` hardcoded constant, parameterless constructor, sync `Assemble` method, null-check branch in `AssembleAsync`; made `_promptStore` non-nullable; changed `ApplyOverflowCascade` and `CalculateTotal` to take required `string systemPrompt` parameter

## Proof Artifacts

| # | Type | File | Status |
|---|------|------|--------|
| 1 | cli | T02.2-01-cli.txt | PASS |
| 2 | file | T02.2-02-file.txt | PASS |
| 3 | file | T02.2-03-file.txt | PASS |
| 4 | cli | T02.2-04-cli.txt | PASS |

## Notes
- Source projects (RunCoach.Api, RunCoach.Poc1.Console) build cleanly with 0 warnings, 0 errors
- Test project compilation fails as expected — ContextAssemblerTests and EvalTestBase reference the removed sync API
- Test fixes are the responsibility of T02.3 (ContextAssemblerTests) and T02.4 (EvalTestBase)
