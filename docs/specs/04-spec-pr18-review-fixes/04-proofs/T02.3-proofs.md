# T02.3 Proof Summary: Convert ContextAssemblerTests to async with mock IPromptStore

## Task
Convert all ContextAssemblerTests to use async `AssembleAsync` with a mock `IPromptStore`, removing references to the deleted sync `Assemble` method and parameterless constructor.

## Changes Made
- **File modified:** `backend/tests/RunCoach.Api.Tests/Modules/Coaching/ContextAssemblerTests.cs`
  - Replaced field initializer `new ContextAssembler()` with constructor that creates a mock `IPromptStore` via NSubstitute
  - Converted 28 sync `Assemble_*` test methods to `async Task AssembleAsync_*`
  - All `_sut.Assemble(input)` calls replaced with `await _sut.AssembleAsync(input, TestContext.Current.CancellationToken)`
  - Removed `AssembleAsync_WithoutPromptStore_FallsBackToHardcodedPrompt` (fallback path no longer exists)
  - Consolidated duplicate `HasFourStartSections` test (YAML variant now redundant with converted test)
  - Simplified existing async YAML tests to use the class-level `_sut` instead of creating per-test instances
  - Kept `EstimateTokens` tests as sync (method is still synchronous)

## Proof Artifacts

| # | File | Type | Status |
|---|------|------|--------|
| 1 | T02.3-01-test.txt | test | PASS |
| 2 | T02.3-02-file.txt | file | PASS |
| 3 | T02.3-03-cli.txt | cli | PASS |

## Result
All 293 tests pass. ContextAssemblerTests.cs builds with zero errors. The 39 test methods (5 sync + 34 async) cover the same behavior as the original 41 methods minus the 2 removed/consolidated tests.
