{"id": "verify", "slice": "Slice 6 PR-A (Settings, regenerate surface, sign out, version) round 2 verify", "spec_path": "docs/specs/slice-6-settings-auth/spec.md (sections 1 B, D; 3 PR-A; 4.2 to 4.6; 5 DU-1 to DU-4; 8)", "diff_base": "HEAD~1", "allowed_commands": "from frontend/: npm run build, npx vitest run <spec>, npx eslint <files>, npx prettier --check <files>; git diff/log/show; NOT runnable here: npm run e2e, npm run check-contrast, full npm run test, dotnet test (record NOT_RUNNABLE_IN_SANDBOX with the error). The orchestrator has ALREADY measured the fixed tree and recorded, in docs/plans/split-ui-cycle/slice-6-evidence/round1/orchestrator-runs.md (fix-round section, copied into this worktree untracked): the full vitest suite, npm run check-contrast, eslint and prettier on the touched files, npm run build, codegen:check, and the Playwright suite against the host stack; the round-1 e2e mutation replays there are all RED; treat those as measured fact. This PR is frontend only.", "lens_task": "Lens: FIX VERIFICATION + MUTATION. HEAD is the fix-round commit; `git diff HEAD~1` is the fix round. Read .fix-brief.txt (the adjudicated fixes F1..Fn) and .stage-report.md (the fixer's claims). For each F1..Fn: confirm the fix is present (file:line) and effective by re-introducing the original bug as a temporary mutation where feasible and recording the observed red; one `mutations` entry per fix with observed RED (verified), GREEN (not effective), NOT_RUNNABLE_IN_SANDBOX (command to orchestrator_runs), or NOT_RUN (say why). List every fix in `conformance` as SATISFIED / PARTIAL / MISSING. Then a fresh pass over the fix-round diff for regressions the fixes introduced. Restore every file you mutate.\n"}
