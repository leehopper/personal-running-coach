F1 (minor; CodeRabbit review thread on PR #364, frontend/src/app/pages/register/register-form.component.tsx line 40,
house rule in frontend/CLAUDE.md Typography: numbers use the condensed font): the register password-rules helper renders
`12` in `font-mono` inherited from the helper's wrapper. Wrap the numeral in `<span className="font-condensed">12</span>`
and keep the rest of the helper copy in mono; the visible text stays `12 characters or more, with an uppercase letter, a
lowercase letter, a digit, and a symbol.` so the existing register spec assertion on the helper text (it matches text
content, not markup) keeps passing. Add one assertion to register.page.spec.tsx that the helper's numeral element carries
`font-condensed` (`renders the helper numeral in the condensed font`). Red: drop the span.
