{"id": "mutation", "slice": "Slice 6 PR-A (Settings recomposition onto the Alpine rule sections, regenerate dialog restyle with the building surface, ACCOUNT with the first SIGN OUT and cache purge, version footer, contrast pairs)", "spec_path": "docs/specs/slice-6-settings-auth/spec.md (sections 1 A, B, D, E; 2; 3 PR-A; 4.1 to 4.6; 5 DU-1 to DU-4 and DU-6; 6; 8; 9)", "diff_base": "HEAD~1", "allowed_commands": "from frontend/: npm run build, npx vitest run <spec>, npx eslint <files>, npx prettier --check <files>; git diff/log/show; NOT runnable here (record NOT_RUNNABLE_IN_SANDBOX with the sandbox's error and put the command in orchestrator_runs): npm run e2e (needs the running stack), npm run check-contrast and the full npm run test (the tsx runner cannot open its IPC pipe), dotnet test. The orchestrator has already run and recorded the full npm run test, npm run check-contrast (27 pairs), codegen:check, npm run build with the injected version, eslint and prettier on every touched file, and the Playwright suite against the host stack in docs/plans/split-ui-cycle/slice-6-evidence/build-orchestrator-runs.md (copied into this worktree untracked); read it and treat those as measured fact. This PR is frontend only: no backend file changed, so dotnet build and dotnet format are not gates here.", "lens_task": "Lens: MUTATION LEDGER. For every new or changed test in the diff, prove it can go red: apply a small reversible mutation to the code it pins (edit the source, run the narrowest test command that covers it, restore the file from `git show HEAD:<path>`), and record observed RED or GREEN in `mutations`. For a behavior the spec pins that has no test, record the mutation you would apply, observed NOT_RUN, and raise a finding. Frontend: `npx vitest run <spec file>` from frontend/. Backend: `dotnet build RunCoach.slnx --no-restore` from backend/ for compile-level claims; no backend test runs in this sandbox (the test host cannot create its named pipe): observe NOT_RUNNABLE_IN_SANDBOX with that error quoted and put the exact `dotnet test --project tests/RunCoach.Api.Tests/RunCoach.Api.Tests.csproj --no-build --filter-class <FullName>` command into orchestrator_runs. Then list, per changed file, what it mutates (state, events, wire shapes, tokens, routes) and attack each mutation: what breaks if the value or order were wrong, and does the code defend it. Finish with every gate the report claims, re-run: `npm run build`, `npx eslint <changed ts/tsx>`, `npx prettier --check <changed>`, `dotnet build --no-restore`, `dotnet format --verify-no-changes` on changed .cs files.\n"}
