{"id": "r5-design-source", "slice": "Slice 6 (design source extraction: Settings, Auth, regenerate dialog, light variants)", "slice_summary": "Slice 6 (Settings & Auth) of the SPLIT / Alpine UI redesign cycle recomposes the Settings screen into the designed rule sections and rebuilds the auth screens as brand posters. Frontend only; no wire, event, prompt, or generated-code change is expected. PR-A Settings (depends on Slice 0 tokens/primitives and Slice 1 tab shell): THE PLAN section with a current-plan line composed client-side from GET /api/v1/plan/current (GeneratedAt, Macro.TotalWeeks, Macro.GoalDescription) rendered as 'Generated Jun 29, 2026 [MIDDLE DOT] 12 weeks [MIDDLE DOT] <goal>', a REGENERATE PLAN clay-outline button and the mono warning 'REPLACES YOUR CURRENT PLAN. THE COACH STARTS FRESH FROM YOUR LOG BOOK.'; the regenerate dialog keeps its 500-character intent textarea and counter, is restyled per design sheet 5h, and adopts the Slice 0 BuildingPlanSurface while regeneration is in flight (today the dialog closes and refetches; this is net-new state wiring); the dead 'View previous plan' placeholder is deleted; APPEARANCE becomes a DARK / LIGHT / SYSTEM SegmentedControl over the existing theme wiring; UNITS becomes a KILOMETERS / MILES SegmentedControl over the existing settings API wiring; ACCOUNT is net-new behavior: 'SIGNED IN AS <email>' plus a SIGN OUT secondary button that calls the existing never-called useLogoutMutation (POST /api/v1/auth/logout), dispatches the logged-out action, posts the cross-tab logout broadcast (the receiver already exists), and calls the RTK api util resetApiState() to purge every per-user cache (DEC-089 D8, the pulled-forward cross-account cache-leak follow-up, declined PR #174), landing on login through the existing guards; footer mono 'SPLIT 0.9.0 [EM DASH] MVP' from a build-time constant derived from the frontend package version (no backend endpoint). PR-B Auth (depends on Slice 0 only): sign-in poster (SPLIT/ 58px wordmark, 64px-wide 2px rule, mono tagline 'THE PLAN ADAPTS. YOU DO THE WORK.', EMAIL and PASSWORD 48px fields, a password visibility eye toggle (net-new behavior), SIGN IN primary, then 'First run here? CREATE ACCOUNT [ARROW]'); register mirrors it with a START HERE heading and the password-rules helper rendered in mono; vertical room stays reserved under the primary button for an OAuth fast-follow and nothing else is built for it. Existing Zod schemas, ProblemDetails error mapping, flows, form ARIA wiring and role=alert messages are preserved contracts. Quality requirements: sign-out gets real tests (mutation call, broadcast reaching a second tab, resetApiState purge asserted by a previously cached query refetching, guard redirect); auth e2e journeys realigned; the regenerate 500-character counter pinned unchanged. Non-negotiables: semantic tokens only, 44px hit targets, 2px clay focus ring, motion-reduce pairing on every animation, dual-theme rendering, AA contrast via check-contrast.", "brief": "Extract the locked design for this slice from the design source so the spec can carry an exact copy table and component map. The design file docs/design/split-alpine/split-alpine.dc.html is one large HTML document with named artboards (screens 2a-2g, light variants 4a and 5a-5h, states 4b). Locate the artboards for 2f Settings, 2g Auth (sign-in and register), 5e Settings light, 5f Auth light, and 5h regenerate dialog (search for these ids or titles; if the naming differs, say what you found and how you matched). For EACH artboard produce: (1) every visible string in reading order (exact characters; mark [EM DASH], [MIDDLE DOT], [ARROW], [ELLIPSIS] where non-ASCII appears), (2) the element geometry that matters to a Tailwind implementer: section rule weight and spacing, label sizes and letter-spacing classes, field heights, button heights and variants (filled clay, clay outline, secondary), the wordmark size and the rule under it, the tagline style, gaps between blocks, the reserved space under the primary auth button, the dialog width and paddings, the counter placement, (3) colors used, each mapped to the semantic token name from frontend/CLAUDE.md's Design tokens section (never a raw hex in the recommendation; if a color has no semantic slot, say so as a gap), (4) states shown (pressed, disabled, focus, error, building). Also read HANDOFF.md sections 1, 2, 3, 5.6, 5.7, 6, 7, 8 and note every place the handoff and the artboard disagree. Recommend: the ordered copy table for Settings, Sign-in, Register, and the regenerate dialog, with the character map and the rule that TSX literals use \\u2014, \\u00B7, \\u2192, \\u2026 escapes; the component map per element (SectionRule, MonoLabel, SegmentedControl, Button variant, Input, Wordmark, BuildingPlanSurface); geometry as Tailwind class guidance; and any element that violates the 44px hit-target rule or the AA contrast commitments so the spec can record the deviation from the mock. Put the complete copy table and component map into draft_artifacts as a markdown file at docs/plans/split-ui-cycle/slice-6-evidence/recon/design-extract.md.", "files": "docs/design/split-alpine/split-alpine.dc.html (the artboards named above; use grep -n on ids, titles, and the strings REGENERATE, SIGN OUT, SIGN IN, START HERE, APPEARANCE, UNITS, ACCOUNT, THE PLAN ADAPTS to find them), docs/design/split-alpine/HANDOFF.md, frontend/CLAUDE.md (Styling, Design tokens & theming, Typography, Alpine components, Animation baseline, Accessibility), frontend/src/index.css (or the token CSS file: find where --alp- and semantic tokens are defined), frontend/src/app/modules/common/components/section-rule/section-rule.component.tsx, frontend/src/app/modules/common/components/mono-label/mono-label.component.tsx, frontend/src/app/modules/common/components/wordmark/wordmark.component.tsx, frontend/src/components/ui/segmented-control.tsx, frontend/src/components/ui/button.tsx, frontend/src/components/ui/input.tsx"}
