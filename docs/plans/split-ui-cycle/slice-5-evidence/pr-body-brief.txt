<task>
Draft a pull request body for the RunCoach repo (your cwd; read AGENTS.md first; read-only except the output file). Match the house shape: read docs/plans/split-ui-cycle/slice-5-evidence/pr-body-example-302.md for the section pattern and register, then write from the brief and the sources below only.
</task>
<brief>
Draft the pull request body for Slice 5 PR-A of the SPLIT / Alpine UI redesign cycle: the backend narrative intake field. Branch feat/slice-5-onboarding-backend, two commits over the evidence commit: the build (6265c38d) and the round-1 fix (ebc49672). Open with one bold sentence naming the PR's place in the slice (PR-A is the backend root; PR-B, the frontend recomposition, follows and consumes the regenerated contract). Then the same sections as the shape example: 'Spec' pointer (docs/specs/slice-5-onboarding/spec.md is gitignored; the committed evidence dir is docs/plans/split-ui-cycle/slice-5-evidence/); 'What's here' (the wire shape and its nullable-not-required swagger proof; the mapper bound 1000 and normalization; the first-event-only AnswerCaptured attachment with tri-state semantics and why not a new event type; the working-view copy that feeds inline plan generation; the state DTO empty-as-null; the composer block bytes and why an empty narrative is byte-identical so no fixture re-record; the new eval scenario and its funded-key record; codegen and the hand-written models); 'Decision record' (DEC-089 D7 build note: manifest regen is a verified no-op because the composer is C#; the backend/REVIEW.md additive-nullable exception and the DEC-091 / Slice 2 precedent; the four cycle-plan Captured rows C1-C4); 'Review trail' (six recon lenses, two-family spec red-team with the counts and the two sol findings rejected, two round-1 lenses, mutation probes M1-M22 all red, one fix round with F1-F3, and that the maintainer's own code-gauntlet run is the cross-family pass still to come); 'Green' (the measured numbers from build-orchestrator-runs.md and round1/orchestrator-runs.md); 'Notes for review' (the ROADMAP/cycle-plan Status edits ride here; the narrative stays unsanitized like the nuance fields by decision; the frontend does not send the field until PR-B). Do not name a Codex model, token count, account, or cost. Do not include a footer; the orchestrator appends the generated-with line.
</brief>
<sources>
- docs/plans/split-ui-cycle/slice-5-evidence/build-report.md (implementer stage report + orchestrator addendum)
- docs/plans/split-ui-cycle/slice-5-evidence/build-orchestrator-runs.md (measured gate outputs)
- docs/plans/split-ui-cycle/slice-5-evidence/round1/fix-list.txt, round1/fix-report.md, round1/orchestrator-runs.md, round1/mutation.json, round1/conformance.json
- docs/plans/split-ui-cycle/slice-5-evidence/redteam/adjudication.md and recon/adjudication.md
- docs/specs/slice-5-onboarding/spec.md sections 1.A, 3 PR-A, 9
- git log -3 and git diff HEAD~2 --stat in this clone (the two PR commits)
- backend/REVIEW.md (Marten section), docs/decisions/decision-log.md DEC-089 D7 build note, docs/plans/split-ui-cycle/cycle-plan.md Captured During Cycle 2026-09-05 rows
</sources>
<grounding_rules>
Use only the sources listed and the repo files they cite. Every claim of what changed cites a path; every number comes from a source. Where sources disagree or leave a gap, say so in the draft under the affected section; never invent a mechanism, a number, or a decision. Do not name a Codex model, token count, or account detail in a PR body. Never read .env, secrets, or ~/.codex.
</grounding_rules>
<style>
Plain prose; no em dashes (use commas or periods); no filler adjectives (comprehensive, robust, seamless, powerful, elegant, crucial, key, vital, significant); one idea per sentence, 25 words maximum; bullets only for parallel items; no bolded-label bullets; end on the last real fact. ASCII only. Under 750 words.
</style>
<output>
Write the draft to docs/plans/split-ui-cycle/slice-5-evidence/pr-body-draft.md. Then print a five-line stage report: sections written, word count, sources used, gaps you could not ground, anything you left out because of the word cap.
</output>
