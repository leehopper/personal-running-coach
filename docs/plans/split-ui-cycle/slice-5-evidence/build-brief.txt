<task>
You are the implementer for Slice 5 PR-A (backend narrative field, prompt injection, eval scenario, hand-written frontend contract) in the RunCoach repo. Your cwd is a local git clone on branch feat/slice-5-onboarding-backend. Read AGENTS.md first (sandbox facts, blocked git, house rules), then in this order before touching anything:
1. docs/specs/slice-5-onboarding/spec.md (sections 1.A, 2, 3 PR-A, 5 Units 1-6, 6, 7, 8) and docs/specs/slice-5-onboarding/pr-strategy.md  (the spec: its Given/When/Then acceptance criteria are your definition of done)
2. docs/plans/split-ui-cycle/cycle-plan.md lines 184-197 and 230-260; docs/plans/split-ui-cycle/slice-5-onboarding.md; docs/plans/split-ui-cycle/slice-5-evidence/recon/adjudication.md (section A is the ruling the spec encodes)  (the cycle plan section and the slice design; the spec wins where they differ)
3. The CLAUDE.md and REVIEW.md files covering the trees you touch (reviewers judge by them)
4. backend/tests/RunCoach.Api.Tests/Modules/Coaching/Eval/PlanGenerationEvalTests.cs lines 440-560 (the dated-event scenario you mirror) and backend/tests/RunCoach.Api.Tests/Modules/Coaching/Eval/EvalTestBase.cs (the Replay/Record guards)
5. backend/src/RunCoach.Api/Modules/Coaching/Models/Structured/MacroPlanOutput.cs and PlanPhaseOutput.cs (the free-text fields the eval concatenates)

Deliver every acceptance criterion in the spec. Files you will create or change (this is the stage-explicit list the orchestrator commits; keep it exact):
backend/src/RunCoach.Api/Modules/Coaching/Onboarding/Models/SubmitStructuredAnswersRequestDto.cs (changed)
backend/src/RunCoach.Api/Modules/Coaching/Onboarding/SubmitStructuredAnswers.cs (changed)
backend/src/RunCoach.Api/Modules/Coaching/Onboarding/SubmitStructuredAnswersRequestMapper.cs (changed)
backend/src/RunCoach.Api/Modules/Coaching/Onboarding/SubmitStructuredAnswersHandler.cs (changed)
backend/src/RunCoach.Api/Modules/Coaching/Onboarding/Events/AnswerCaptured.cs (changed)
backend/src/RunCoach.Api/Modules/Coaching/Onboarding/OnboardingView.cs (changed)
backend/src/RunCoach.Api/Modules/Coaching/Onboarding/OnboardingProjection.cs (changed)
backend/src/RunCoach.Api/Modules/Coaching/Onboarding/Models/OnboardingStateDto.cs (changed)
backend/src/RunCoach.Api/Modules/Coaching/Onboarding/OnboardingController.cs (changed)
backend/src/RunCoach.Api/Modules/Coaching/ContextAssembler.cs (changed)
backend/tests/RunCoach.Api.Tests/Infrastructure/RecordingPlanGenerationService.cs (NEW: IPlanGenerationService decorator over an inner service that records the last OnboardingView passed to GeneratePlanAsync; registered only for the one test that needs it, following the RejectOnceThenSucceedPlanGenerationService precedent)
backend/tests/RunCoach.Api.Tests/Modules/Coaching/Onboarding/SubmitStructuredAnswersRequestMapperTests.cs (changed)
backend/tests/RunCoach.Api.Tests/Modules/Coaching/Onboarding/OnboardingProjectionTests.cs (changed)
backend/tests/RunCoach.Api.Tests/Modules/Coaching/Onboarding/OnboardingAnswersEndpointIntegrationTests.cs (changed)
backend/tests/RunCoach.Api.Tests/Modules/Coaching/Onboarding/ContextAssemblerPlanGenerationTests.cs (changed)
backend/tests/RunCoach.Api.Tests/Modules/Coaching/Eval/PlanGenerationEvalTests.cs (changed: the new Replay-skipping scenario)
frontend/src/app/modules/onboarding/models/onboarding.model.ts (changed: `narrative?: string | null` on both hand-written envelope types, optional so existing spec fixtures keep compiling)
Do not touch any other file. If an acceptance criterion forces another file, stop and say so in the report instead.
</task>

<environment_and_gates>
- Scratch: create `/tmp/codex-agent-feat/slice-5-onboarding-backend` once and set `TMPDIR=/tmp/codex-agent-feat/slice-5-onboarding-backend` inline on every command that needs it; each command runs in a fresh shell, so an export does not persist. Everything under that directory (backups, the plan in step 1) is scratch, not a deliverable, and is outside the file list.
- Gates, run before you report: from backend/ `dotnet build RunCoach.slnx --no-restore` and `dotnet format RunCoach.slnx --include <changed .cs> --no-restore --verify-no-changes`; from frontend/ `npm run build`, `npx vitest run <each changed or added spec>`, `npx eslint <changed .ts/.tsx>`, `npx prettier --check <changed>`. No backend test runs in this sandbox: the test host cannot create its named pipe (SocketException 13). For every test you add or change, name the exact `dotnet test --project tests/RunCoach.Api.Tests/RunCoach.Api.Tests.csproj --no-build --filter-class <FullName>` command for the orchestrator under Deviations, with that error as the reason; never claim a test result.
- Git is blocked (leave changes in the working tree). Never delete or move a tracked file unless the file list says so. Backups go to that scratch directory via `/bin/cp -f`; the command policy rejects `rm -f`, so delete scratch with plain `rm <path>`.
- ASCII only in new prose and comments; no em dashes. Comments never cite a plan, spec, or decision path.
- Generated artifacts are NOT yours: do not touch backend/openapi/swagger.json, frontend/src/app/api/generated/**, or the generated barrel. The orchestrator runs the Release OpenAPI emission and `npm run codegen` after your stage and commits them with your files. Write the hand-written frontend model change so it compiles against BOTH the current generated types and the regenerated ones (an additive optional property).
- Do not edit any file under backend/src/RunCoach.Api/Prompts/ and do not touch the eval cache. The narrative block is C# composition only (spec section 1.A); the DEC-074 manifest must be unchanged, and `bash backend/tests/scripts/check-prompt-hashes.sh` must still pass (run it and quote its last line).
- No EF migration, no model-snapshot edit, no Marten registration change: the event field is nullable-additive with a default.
- The new eval test must compile and must skip in Replay while its fixture is absent, mirroring `DatedEvent_Macro_LandsRaceWeekInFinalPhase` exactly (same `CanRunEvals` and `SonnetFixtureExists` guards). Never call the network. The orchestrator records the fixture.
- Every test you add must be able to fail against the gap it names; for each, write in the Mutations section the one-line mutation that would turn it red.
- The endpoint integration tests run only from the orchestrator's session (Docker). Make them compile and follow the existing harness patterns in the same file (antiforgery priming, `PostAnswersAsync`, the shared fixture, tenant handling); hand the exact filter commands over under Deviations.
- Keep the ContextAssembler change to the smallest diff: one `if` block before the `PROFILE SNAPSHOT` line, with the exact label bytes from spec section 1.A; update the method's XML summary in one sentence.
</environment_and_gates>

<method>
1. Read everything listed. Write `/tmp/codex-agent-feat/slice-5-onboarding-backend/plan.md` with the exact edit list and the tests you will add per acceptance criterion, then implement.
2. Tests first where the spec pins a behavior: each new test must fail against the bug or gap it names; note that red in the report.
3. Keep every change inside the file list. Prefer the smallest diff that satisfies the criterion; match the surrounding code's conventions rather than introducing new patterns.
4. Run every gate above; paste each command's last output line.
</method>

<completeness_contract>
Every acceptance criterion gets a line: PASS with the command and its last output line, or DEVIATION with what and why. No criterion may be silently skipped. The report is your final message, markdown, sections: Files (exact paths, new vs changed), Gates (each command and its last line), Acceptance table, Mutations (each new test and the bug it fails against), Deviations (including every command the orchestrator must run for you), Open questions. Finish with the line STAGE COMPLETE.
</completeness_contract>
