{
 "artifact_kind": "the Slice 5 (Onboarding) implementer's-contract spec (two markdown files)",
 "shape_example_path": "docs/specs/slice-4-log-logbook/spec.md (and docs/specs/slice-4-log-logbook/pr-strategy.md for the companion)",
 "brief": "Write the Slice 5 (Onboarding) implementer's-contract spec for the SPLIT / Alpine UI redesign cycle as TWO files:\n(1) docs/specs/slice-5-onboarding/spec.md (the output path below) and (2) docs/specs/slice-5-onboarding/pr-strategy.md\n(a short companion, 400 words max, modeled on docs/specs/slice-4-log-logbook/pr-strategy.md: principles, the two-PR dependency\ngraph PR-A backend root -> PR-B frontend, merge order, and the rule that the user merges).\n\nThe spec is a contract for a Codex implementer who will not re-derive anything: every file path, symbol, property name, JSON\nname, class string, data-testid, copy string, and algorithm is locked. Follow the Slice 4 spec's section pattern exactly:\n1. Decision record (the rulings in recon/adjudication.md sections A and B, each with its rationale and the alternatives the recon\n   surfaced and the adjudication rejected: a new event type, duplicating the narrative on every event, an EF profile column,\n   the 12-week copy, ADD DETAIL in THE RACE, rotating the idempotency key on 422);\n2. References (ground truth): a table of every concern -> file : symbol with line ranges taken from recon/out.json facts, plus a\n   \"Confirmed empirically this pass\" list of the load-bearing facts (the composer is C#, not YAML, so no manifest change; empty\n   narrative is byte-identical; AnswerCaptured is one event per topic; the POST is synchronous so building shows during isLoading;\n   the generated barrel needs no edit; BuildingPlanSurface props; the E2E stubs plan generation);\n3. Build stages: PR-A (backend) and PR-B (frontend), each with an exact file list (new vs changed vs deleted), the edit shape per\n   file, and the tests to add per file (test class and method names). PR-A's list must include the generated files\n   (backend/openapi/swagger.json, frontend/src/app/api/generated/rtk/api.ts, frontend/src/app/api/generated/zod/onboarding/onboarding.ts),\n   the hand-written frontend models file, and the new eval scenario. Name the orchestrator-only steps (dotnet test, the targeted\n   Record with the funded key, the entry.json TTL patch, Replay verify, the codegen chain, the E2E run) as \"Orchestrator runs\".\n4. Client contracts (locked): the copy table (every visible string, in order, per section, with the [EM DASH]/[MIDDLE DOT]/[ELLIPSIS]\n   character map and the rule that TSX literals use \\u2014, \\u00B7, \\u2026 escapes), the component map for sections 00-05\n   (primitive per element, class strings from recon r4 config entries where relevant, testids, accessible names), the narrative\n   Zod/request/hydration/reseed shape, the building-overlay behavior, the unit-token label swap;\n5. Demoable units and BDD acceptance criteria: Given/When/Then per unit, each naming the test file or command that scores it\n   (backend test class.method; vitest spec file and test name; the eval scenario; the E2E spec). Cover: narrative round-trip\n   (POST -> event -> view -> GET state -> prompt), empty and whitespace narrative, 1000/1001 bound, legacy event replay, byte-identical\n   composition, the eval scenario, swagger shape; the recomposed intake in both themes, race reveal, injury reveal, ADD DETAIL\n   placement, day chips, switches, units segmented first, building overlay during in-flight and after completion, 422 returns\n   the form with values intact and the same key, resume hydration incl. narrative, reseed on units change, idempotency rotation\n   unchanged;\n6. Edge-case matrix;\n7. Test plan (backend, frontend unit, component, E2E realignment list, contrast, codegen drift, manifest unchanged, eval Replay);\n8. Non-negotiables checklist (HANDOFF section 8, trademark rule, AGENTS.md rules: source-generated logging, no doc paths in\n   comments, ASCII, motion-reduce pairing, semantic tokens only, --alp-faint never on essential text, 44px hit targets);\n9. Open questions / flagged deviations, each with the adjudicated recommendation (B2 horizon copy, B3 helper variant, B4 05 label,\n   C1-C3 follow-ups);\n10. Out of scope (deferred).\n\nGround every claim in recon/out.json facts (cite file:line as the facts do) or in the adjudication. Where the adjudication and a\nrecon fact conflict, the adjudication wins and you say so in one sentence. Where neither settles a detail an implementer needs\n(for example the exact MacroPlanOutput free-text field names for eval assertion (3), or the .t-* typography utility for the h1),\nread the repo file and lock it, citing the line. Do not reopen decisions. Do not invent copy: every UI string comes from the r6\ndraft table or from adjudication B. The design's row/grid/chip geometry (r6 layout facts) becomes Tailwind class guidance, not\npixel prose. Write the acceptance criteria so each can fail before the build and pass after it.",
 "sources": "- docs/plans/split-ui-cycle/slice-5-evidence/recon/adjudication.md (the orchestrator's rulings; authoritative)\n- docs/plans/split-ui-cycle/slice-5-evidence/recon/out.json (six recon lenses: facts with file:line, config entries, risks, gaps; r6 carries the ordered copy table and the design geometry)\n- docs/plans/split-ui-cycle/slice-5-onboarding.md (slice design, D1-D5)\n- docs/plans/split-ui-cycle/cycle-plan.md lines 184-197 (Slice 5 acceptance), 230-260 (architecture additions, testing strategy)\n- docs/design/split-alpine/HANDOFF.md sections 5.5, 6, 7, 8\n- docs/decisions/decision-log.md: DEC-086 (line 3059), DEC-087 (3088), DEC-088 (3113), DEC-089 (3141; D7 at 3157), DEC-074 (2738), DEC-039 (1023)\n- AGENTS.md, backend/CLAUDE.md, frontend/CLAUDE.md, REVIEW.md files (house rules the implementer is judged by)\n- Repo files named in the recon facts, for any detail you must lock yourself",
 "word_cap": "9000",
 "out_path": "docs/specs/slice-5-onboarding/spec.md (and docs/specs/slice-5-onboarding/pr-strategy.md)"
}