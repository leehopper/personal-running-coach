# Research Queue

Topics to hand off to a deep research agent. Each entry captures the question, why it matters, and where findings should feed back into the planning docs. Full research artifacts live in `research/artifacts/` — only outcomes and key takeaways get pulled into the main planning documents.

## How to Use

1. Add a topic below with a clear research question and context on why it matters.
2. Hand off to a deep research agent.
3. Store the full output in `artifacts/` (e.g., `artifacts/competitive-landscape.md`).
4. Summarize key takeaways back into the relevant planning doc and update status here.

---

## Priority Ordering

Research is batched by dependency. Later batches depend on findings from earlier ones.

- **Batch 1 (Kick off now — unblocks everything else):**
  R-008 + R-009 (dev tooling & workflow) — shapes how you run every other research task and POC.
  R-002 (competitive landscape) — validates the problem space before investing in solutions.

- **Batch 2 (Kick off after Batch 1, or in parallel if capacity allows):**
  R-001 + R-004 (training methodologies + planning architectures) — directly feeds POC 1 and POC 3.
  R-007 (testing non-deterministic outputs) — needed before POC work so you can evaluate results.

- **Batch 3 (Important but not blocking POCs):**
  R-003 (safety/liability), R-005 (BYOM + model strategy), R-006 (wearable integrations).

- **Batch 4 (Discovered topics — feeds prompt engineering and safety testing):**
  R-010 (coaching conversation design), R-011 (special populations safety boundaries).

---

## Queue

| # | Topic | Research Question | Why It Matters | Status | Artifact |
|---|-------|-------------------|----------------|--------|----------|
| R-001 | Training methodologies as configurable frameworks | What are the major running training methodologies (Daniels, Hanson, Pfitzinger, 80/20, etc.), how do they differ in structure and philosophy, and how could an AI select and blend between them based on user profile and feedback? | Core to the product — the AI needs a principled basis for plan generation, and users benefit from methodology choice. Also need to understand where LLMs are strong vs. weak on training science. | Integrated | batch-2a-training-methodologies.md |
| R-002 | Competitive landscape & market opportunity | What do existing AI running coaches and adaptive training tools (TrainAsONE, COROS coach, Garmin Coach, Runna, etc.) actually do? Where do they fall short? What do users complain about? Does a truly adaptive AI coaching product already exist? If not, why — is it a market gap or is there a structural reason it hasn't been built? | Validates the problem space, identifies differentiation opportunities, and answers the fundamental "why doesn't this exist yet" question. Could reveal barriers we haven't considered. | Integrated | batch-1b-competitive-landscape.md |
| R-003 | Safety, liability, and legal guardrails | What are the real risks of AI-generated training plans (overtraining, injury, cardiac events, etc.)? What is the legal liability if a user is harmed following AI-generated advice? How do existing fitness apps handle this (disclaimers, waivers, limits on advice)? Given that users can already get similar advice from raw AI chat, what is the incremental liability of packaging it as a product? | Critical for understanding worst-case exposure. Directly informs what guardrails are legally necessary vs. nice-to-have, and whether the product needs medical disclaimers, age restrictions, or health screening gates. | Integrated | batch-3a-safety-liability.md |
| R-004 | AI-driven planning architectures and tiered plan models | What design patterns exist for AI systems that manage multi-layered, evolving plans? How do other AI-first products (not just running) handle the tension between structured plan state and flexible AI output? Is macro/meso/micro the right decomposition, or are there better models? | The three-tier model is a core assumption. Need to validate it against real-world patterns before it becomes load-bearing in the data model. Directly feeds into POC 3. | Integrated | batch-2b-planning-architecture.md |
| R-005 | BYOM (Bring Your Own Model) and model dependency strategy | Is it feasible to let users bring their own API key (OpenAI, Anthropic, etc.) for unlimited querying, alongside a default in-house model with usage limits? What are the UX, security, and architectural implications? How do other AI-first products handle this (e.g., Cursor, Typingmind, OpenRouter)? Additionally: should the product be model-agnostic or optimized for one provider? What's the cost of supporting multiple models (prompt portability, behavior differences, testing overhead)? | Directly shapes the monetization model and architecture. BYOM could eliminate the cost barrier for power users, but adds complexity. Model-agnostic design provides resilience against provider changes but costs more upfront. These questions are intertwined and should be researched together. | Integrated | batch-3b-byom-model-strategy.md |
| R-006 | Wearable & platform integration feasibility | What does integration with Garmin Connect, Apple Health, Strava, and similar platforms actually require? What APIs are available, what are the approval processes, rate limits, and data access restrictions? Which platforms are easiest to integrate first? | The app's value increases dramatically with automatic data ingestion. Need to understand the real effort and constraints for each platform before committing to an integration roadmap. | Integrated | batch-3c-wearable-integrations.md |
| R-007 | Testing non-deterministic AI outputs | How do you evaluate the quality of AI-generated training plans when outputs are non-deterministic? What scoring criteria, rubrics, or automated evaluation frameworks exist for assessing plan quality? How can you set thresholds for "good enough" coaching output? | Essential for both development confidence and ongoing quality assurance. Without a way to measure output quality, you can't tell if prompt changes or model updates are making things better or worse. Feeds directly into POC work. | Integrated | batch-2c-testing-nondeterministic.md |
| R-008 | Claude Code best practices, plugins, and agent frameworks | What are the current best practices for building a full product with Claude Code? Investigate "everything claude code," "awesome-claude-code," and similar community resources. What skills, plugins, agent team patterns, and frameworks exist? How to maximize AI-assisted development — multi-agent workflows, custom skills, MCP servers, etc. Focus on what's available for personal/open-source use (not proprietary). | This is the development methodology for the entire project. Getting the tooling right early compounds across every phase of building. Want to leverage the cutting edge of what's possible with Claude Code rather than using it as a basic coding assistant. | Integrated | batch-1-claude-code-workflow.md |
| R-009 | AI-assisted side project workflow and context management | How do solo builders / side project developers organize AI-assisted development across project phases? Specifically: (1) How to maintain a clean separation between planning/ideation docs and active codebase so ongoing ideas don't pollute the build. (2) How to hand off rich context from one phase to the next (e.g., planning → POC → MVP → post-MVP) so each new agent session doesn't start cold. (3) What repo structures, CLAUDE.md patterns, multi-workspace setups, or project scaffolding approaches exist for this? (4) How to run autonomous agents effectively on a side project — what guardrails and structures produce good outcomes without constant supervision? Look at how the Claude Code community handles this, what conventions are emerging, and whether existing frameworks (agent-stack, claude-flow, etc.) solve parts of this. | This is the operational problem for the entire project. Building is a side project with limited time — need to maximize what autonomous agents can accomplish per session. The planning docs we've built are the first layer, but need a clear bridge from "organized planning docs" to "agent can pick this up and build the next phase." Without this, each session starts from scratch and planning/ideation work gets tangled with implementation. | Integrated | batch-1-claude-code-workflow.md |

| R-010 | Coaching conversation design & behavioral psychology | How do effective human running coaches communicate plan changes, setbacks, and corrections? What does sports psychology research say about motivation, adherence, and tone in coaching relationships? How should an AI coach handle common behavioral challenges: users who run too hard on easy days, users who are demoralized by a downgrade, users who ignore advice? What conversational patterns build trust vs. erode it? | R-001 and R-004 defined what the AI adapts — but not how it talks. The coaching persona prompt is the user-facing product. Getting the tone wrong (too clinical, too cheerful under injury, too scolding on over-performance) directly undermines trust and retention. This feeds prompt engineering for the coaching layer. | Integrated | batch-4a-coaching-conversation-design.md |
| R-011 | Special populations & safety edge cases | What are the safety boundaries and coaching considerations for populations the AI will encounter but isn't specifically designed for? Specifically: pregnancy/postpartum running, menstrual cycle effects on training, masters runners (age-related adaptation), juvenile runners, runners with chronic conditions (asthma, type 1/2 diabetes, cardiac arrhythmias), and return-to-run protocols after common injuries (plantar fasciitis, IT band, stress fractures, Achilles tendinopathy). For each: what should the AI know, what should it refuse to advise on, and what should trigger a "see a professional" referral? | The adversarial test library (DEC-016) needs comprehensive safety scenarios. Without researching these populations, the AI might give dangerous advice to someone who mentions they're pregnant, diabetic, or recovering from a stress fracture. This isn't about building features for these groups — it's about knowing the boundaries so the AI stays safe. Directly feeds the safety scenario library and system prompt guardrails. | Integrated | batch-4b-special-populations-safety.md |

| R-012 | AI-powered PR review and code quality tooling for AI-assisted development | What are the current AI-powered PR review tools (CodeRabbit, Anthropic's Claude Code Review GitHub Action, Codacy, Qodo, etc.)? How do they compare for a solo developer whose code is primarily AI-generated? What pre-commit hook ecosystems, static analysis tools, and CI quality gates best complement AI code generation? How should the full quality pipeline be designed when the "author" is an AI agent? | The project uses Claude Code as the primary development tool. All code is AI-generated and needs deterministic quality enforcement at multiple layers (edit-time hooks, pre-commit, PR review, CI). AI-reviewing-AI is a different use case than AI-reviewing-human — the failure modes and value proposition differ. This directly feeds the quality gate pipeline design (DEC-034) and Claude Code hook configuration. | Integrated | batch-5-ai-pr-review-quality-tool.md |

| R-013 | LLM eval test strategies for non-deterministic output assertion | How do teams structure eval suites for non-deterministic LLM outputs? What are the best practices for structured output enforcement, LLM-as-judge patterns, tiered assertion strategies (deterministic → NLI → judge), and statistical approaches to flaky eval tests? | POC 1 eval tests use brittle string matching and JSON key guessing. Need a principled testing architecture before building out the full eval suite. Directly informs the LLM testing refactor on PR #17. | Integrated | batch-6a-llm-eval-strategies.md |
| R-014 | .NET libraries for LLM response caching, HTTP recording/replay, and eval tooling | What .NET libraries exist for HTTP recording/replay (VCR pattern), LLM-specific evaluation (Microsoft.Extensions.AI.Evaluation), structured output schema generation, snapshot testing, and test parallelization with rate limits? | Need concrete library choices for the .NET eval test infrastructure. Discovered Microsoft.Extensions.AI.Evaluation as the primary .NET eval framework. Directly informs DEC-036. | Integrated | batch-6b-dotnet-llm-testing-tooling.md |
| R-015 | IChatClient bridge gap for Anthropic structured outputs | The Anthropic SDK's IChatClient bridge silently drops ChatResponseFormat.ForJsonSchema(). How to bridge structured output through the M.E.AI pipeline? What's the DelegatingChatClient pattern? | Discovered during POC 1 eval implementation. Without the bridge, structured output calls return free-form JSON. Feeds DEC-037. | Integrated | batch-7a-ichatclient-structured-output-bridge.md |
| R-016 | Anthropic model IDs, versioning strategy, and structured output compatibility | Which models support structured output? How do floating alias IDs work? What's the recommended config pattern to avoid hardcoded model IDs? | Model ID confusion blocked eval tests. Old Sonnet 4 doesn't support structured output. Feeds DEC-037 and DEC-038. | Integrated | batch-7b-anthropic-model-ids-versioning.md |
| R-017 | LLM eval cache management for CI — TTL strategy and best practices | M.E.AI's DiskBasedCacheOptions hardcodes 14-day TTL. How do teams manage committed LLM eval fixtures for CI replay? What are the M.E.AI configuration options, industry patterns, and workarounds? | Committed cache fixtures expire silently, breaking CI. Need a TTL strategy for golden fixtures. Feeds DEC-039. | Integrated | batch-8a-eval-cache-ttl-ci.md |
| R-019 | Daniels' Running Formula pace table verification and edition consistency | Verify VDOT 50 pace data (anomalous 2-3x jump at 49→50 boundary), check edition consistency (VdotCalculator uses 3rd ed, PaceCalculator uses 4th ed), validate per-mile to per-km conversion methodology, cross-reference against online calculators and known errata. | Code review found a data anomaly that produces incorrect training paces for runners at VDOT 49-50. Also discovered edition mismatch between the two calculators. Directly blocks PR #17 merge. | Done | batch-9a-daniels-pace-table-verification.md |
| R-020 | Unit system design for metric/imperial/mixed contexts | How should a running app handle metric vs imperial vs mixed units (track meters, road miles/km, race names always metric)? Internal storage strategy, conversion architecture, LLM interaction, wearable data ingestion, user preference design, phased implementation. | Current codebase is metric-only with no conversion layer. PreferredUnits is a pass-through string. Need a clean foundation before MVP-1 adds imperial users. Feeds future DEC on unit architecture. | Done | batch-9b-unit-system-design.md |
| R-021 | Frontend library versions and code review best practices (2026) | Validate current React 19 + TypeScript frontend is on latest stable versions. Establish code review rules for React 19, TypeScript 5.9, Vite 8, Tailwind v4, Zod v4, RTK Query, React Hook Form, Vitest 4, ESLint v9 flat config. | Frontend scaffolding complete but no feature modules yet. Need validated version baseline and comprehensive REVIEW.md rules before feature development begins. Feeds REVIEW.md generation. | Done | 10a-frontend-latest-practices.md |
| R-022 | .NET 10 / C# 14 backend code review best practices | Establish comprehensive code review rules for .NET 10, C# 14, ASP.NET Core, EF Core, Marten, xUnit v3, FluentAssertions, Anthropic SDK, M.E.AI.Evaluation. Cover async discipline, performance anti-patterns, security, testing, LLM integration patterns. | Backend has significant POC 1 code. Need REVIEW.md rules that catch .NET-specific issues, cross-referenced with Microsoft dotnet-diag/dotnet-data/dotnet-msbuild skill anti-pattern catalogs. | Done | 10b-dotnet-backend-review-practices.md |
| R-023 | CI/CD pipeline and quality gates for private repos | GitHub Actions best practices, Trivy config, Codecov strategy, Lefthook optimization, Docker Compose + Tilt, Dependabot strategy, quality gates without branch protection, AI-generated code concerns. | Private repo constraints limit available tooling (no CodeRabbit, CodeQL, SonarCloud, branch protection). Need to maximize quality within these constraints. Feeds root REVIEW.md CI rules. | Done | 10c-ci-quality-gates-private-repo.md |
| R-024 | REVIEW.md configuration best practices for AI code review | How to write effective natural-language review rules for AI agents. Rule specificity, hierarchy design, skip patterns, threshold tuning, signal-to-noise, rule maintenance. Cross-reference CodeRabbit, ESLint, Greptile, SonarQube patterns. | First time configuring REVIEW.md. Need a framework for writing high-signal rules that reduce false positives while catching real issues. Feeds all 3 REVIEW.md files. | Done | 10d-review-md-configuration-practices.md |
| R-025 | Durable Daniels pace-zone implementation pattern and library survey | What is the best way to implement Daniels' Running Formula pace-zone calculations in .NET so values are authoritative, self-verifying, and extensible? Survey existing libraries (GoldenCheetah, tlgs/vdot, Run SMART Project, mekeetsa/vdot, any NuGet option). Nail down the per-zone derivation methodology from first-party book sources — especially Repetition, which is where our 2026-04-14 audit found the biggest residual anomaly. Establish a reference-grade VDOT 30-85 table sourced directly from Daniels 4th edition. Compare implementation patterns (pure-equation, equation-verified lookup, committed fixture, port, library dependency) and recommend one. | DEC-040 shipped a partial fix by row-shifting pre-existing data, not by re-deriving from the book. A 2026-04-14 computational audit found residual discontinuities at VDOT 49→50 in the Interval (+1.55pp) and Repetition (+3.69pp) columns — R-019 explicitly did not verify VDOT 30-49, and the row-shift fix preserved any secondary error in that range. Need a durable, library-or-pattern-based solution that eliminates the transcription error class entirely and is self-auditing going forward. R-025 findings drive the real design fix (DEC-042). | Done (has errata) | batch-11-daniels-implementation-patterns.md |
| R-026 | Daniels-Gilbert equations complete implementation reference | Capture the full mathematical foundation: both equations, all eight coefficients, forward VDOT computation, quadratic inversion for velocity from VO₂, Newton-Raphson race-time prediction, hybrid zone-derivation strategy, validated ranges and model-breakdown regions. Originally scoped as "4th edition pace-table capture" but pivoted by the research agent to equation-based derivation due to copyright concerns about verbatim book reproduction. | Pivot turned out to be the right call — R-034 independently confirms that equation-derived fixtures are the legally and technically correct foundation. This artifact is now the primary mathematical reference for DEC-042's implementation. | Done | batch-12a-daniels-gilbert-equations.md |
| R-027 | Daniels-Gilbert equation coefficient stability across editions | Verify the eight coefficients are identical across all four book editions, the 1979 monograph, and vdoto2.com's live calculator. Check for any Human Kinetics errata or Daniels public statements about coefficient revision. | Both `VdotCalculator` and the proposed pure-equation `PaceCalculator` depend on coefficient stability. R-025 claimed stability but didn't verify against the 4th edition or vdoto2.com directly. | Done | batch-12b-equation-coefficient-stability.md |
| R-028 | Exact derivation for fixed-% zones (E, T, I) | Nail down the exact %VO₂max values for Threshold, Interval, and Easy-range fast/slow. R-025's claims (I = 100%, E = 59–74%) don't reconcile with book values. Need ±0.5 pp precision or an iterative race-prediction alternative. | A 3% error in %VO₂max translates to ~5 s/km of systematic bias. Implementation correctness hinges on pinning these constants or identifying that one of them is a race-time prediction in disguise. | Done (has errata) | batch-12c-pace-zone-derivation-constants.md |
| R-029 | Zone completeness and VDOT continuity semantics | Does the 4th edition publish exactly five training zones or additional/sub-zones (cruise intervals, stride, Fast Reps, long-run, recovery)? Are sub-distances (T-400, T-1000, T-mile) independently derived? Does the book define non-integer VDOT semantics? | Five-zone assumption drives the calculator shape. Linear interpolation between integer rows is mathematically unjustified given nonlinear equations. Need to know whether continuous-VDOT is the intended semantic. | Done | batch-12d-zone-completeness-vdot-continuity.md |
| R-030 | VdotCalculator verification against 4th edition race-time table | Audit the existing `VdotCalculator.CalculateVdot(raceTime)` implementation against the book's race-time-to-VDOT table across multiple distances and fitness levels. Same transcription-error risk class as PaceCalculator. | A broken race-time-to-VDOT mapping silently poisons every downstream pace calculation. Never audited end-to-end since implementation. | Done | batch-12e-vdotcalculator-verification.md |
| R-031 | Max HR formula and HR-zone derivation in the 4th edition | What max-HR formula does Daniels 4th edition recommend, and how does he define HR-based training zones alongside pace zones? `EstimateMaxHr(age) = 220 − age` is known outdated. | `EstimateMaxHr` lives in `PaceCalculator.cs` and is scope-adjacent to DEC-040. Worth resolving in the same rewrite. Also need to know whether HR zones are a separate calculator or coupled. | Done | batch-12f-hr-zones-and-max-hr.md |
| R-032 | Multi-methodology support — will RunCoach need non-Daniels zones? | Does RunCoach's product roadmap need to support training methodologies other than Daniels (Hanson, Pfitzinger, 80/20, polarized, MAF, Coggan) in MVP-1 or later? What interface shape future-proofs the calculator? | Designing a `PaceZoneCalculator` tightly coupled to Daniels-Gilbert is clean but may paint us into a corner. Getting the abstraction shape right now is cheap; retrofitting later is not. | Queued (deferred — not blocking DEC-042) | (pending) |
| R-033 | LLM pace-zone consumption precision requirements | How does the coaching LLM consume pace zones — raw numbers, narrative descriptions, categorical intensities? Does sub-5 s/km precision measurably change plan quality in the eval suite? | Drives the precision budget for the whole calculator. If the LLM uses narrative ranges only, sub-second precision is wasted effort. | Queued (deferred — informs DEC-041 integration, not DEC-042 correctness) | (pending) |
| R-034 | Copyright and licensing for derived Daniels pace tables | If we commit a CSV of Daniels 4th-edition pace values as a golden test fixture, is it fair use, derivative work, or infringement? How do existing OSS projects handle this legally? What about querying vdoto2.com manually for calibration? | RunCoach is private now but may go public. Don't want to bake in copyright liability or discover we need to rewrite the fixture before open-sourcing. | Done | batch-12g-copyright-licensing.md |
| R-035 | R-pace derivation — mile vs 3K formulation disambiguation | Resolve direct conflict between R-025 (R = predicted mile race pace) and R-028 (R = predicted 3K race pace × distance-specific multipliers 0.9295/0.9450/0.9528). Use vdoto2.com manual queries as oracle, GoldenCheetah's `VDOTCalculator.cpp` as reference implementation. Compute both formulations across VDOT 30–85, error-analyze against oracle, recommend one. Bonus: F (Fast Repetition) zone formula. | Both formulations produce correct R-400 ≈ 87 s at VDOT 50 but may diverge elsewhere; neither artifact gave a side-by-side comparison. R-pace is the last remaining unresolved gap before DEC-042 implementation can proceed. | Done | batch-13-r-pace-disambiguation.md |
| R-036 | CodeRabbit integration for a .NET 10 + React 19 monorepo (public OSS) | Produce a ready-to-commit `.coderabbit.yaml` and install plan. Pin schema v2, confirm free-forever Pro tier for public repos, protect `backend/**/Prompts/*.yaml` coaching IP from AI rewording, silence Dependabot PR noise, prevent double-reporting against Roslyn/StyleCop/SonarAnalyzer.CSharp, keep ADRs free of line-level nitpicks. | DEC-034 private-repo amendment stripped CodeRabbit from the pipeline in 2026-03-19; the 2026-04-15 public flip is the restoration trigger. CodeRabbit is the "cross-model PR review" layer DEC-034's original research valued most, and needs durable file-based config that new modules inherit automatically. Feeds OSS Quality Tooling Restoration in ROADMAP Next Up §3. | Done | batch-14a-coderabbit-integration.md |
| R-037 | CodeQL SAST for a .NET 10 + React 19 monorepo with `.slnx` solution | Produce a ready-to-commit `.github/workflows/codeql.yml` + `.github/codeql/codeql-config.yml`. Verify current major (v3 vs v4), autobuild `.slnx` support, combined `javascript-typescript` identifier, `security-extended` suite, coaching-prompt path exclusions, branch-protection soft-launch, and storage/retention on public repos. | First-party SAST is the one security signal Trivy does not produce — Trivy covers dep CVEs, secrets, and IaC but cannot find a SQL-injection sink or XSS path in our own code. Restoring CodeQL fills that gap and partitions cleanly from SonarAnalyzer.CSharp build-time rules (advisory at build, hard-gated here). Feeds ROADMAP Next Up §3. | Done | batch-14b-codeql-integration.md |
| R-038 | SonarQube Cloud CI-based analysis for .NET 10 + React monorepo | Produce a ready-to-commit `sonarqube.yml` workflow, `sonar-project.properties` per module, and one-time org/GitHub-App setup checklist. Verify product rename (SonarCloud → SonarQube Cloud), current action (unified `sonarqube-scan-action` vs legacy `sonarcloud-github-action`), automatic vs CI-based recommendation for monorepos, Cobertura vs OpenCover for .NET coverage ingestion, one-vs-two-project structure, quality-gate customization, and MCP server integration for Claude Code. | Provides the trend/duplication/cognitive-complexity dashboard the build-time `SonarAnalyzer.CSharp` + `eslint-plugin-sonarjs` stack does not, plus cross-file taint and PR decoration. Sonar + CodeQL together replace DEC-034's Layer 3 + Layer 4. MCP server integration (user-side) is the meaningful upgrade for Claude Code workflows. Feeds ROADMAP Next Up §3. | Done | batch-14c-sonarqube-cloud-integration.md |
| R-039 | Snyk re-evaluation against Dependabot + Trivy + CodeQL (public repo, 2026 free tier) | Re-run the DEC-034 Snyk rejection decision under the inverse trigger (public flip). Document 2026 free-tier limits for public repos, fix-PR behavior vs Dependabot, transitive-dependency handling, @snyk/protect patch status, SCA overlap matrix (Snyk OSS vs Dependabot + Trivy), SAST overlap matrix (Snyk Code vs CodeQL), account-friction install steps. Recommend adopt / skip / defer with reconsider-triggers. | DEC-034 rejected Snyk with "reconsider if repo goes private." The public flip is the inverse trigger and warrants an explicit yes/no decision rather than a blind restore. Result feeds either a Snyk workflow in ROADMAP Next Up §3 or the explicit deferred-with-reconsider-trigger entry already drafted in the deferred-items section. | Done | batch-14d-snyk-reevaluation.md |
| R-040 | Codacy re-evaluation against current stack + CodeQL + SonarQube Cloud | Re-run the DEC-034 Phase-2 Codacy decision under current 2026 free-tier terms. Produce a signal-by-signal overlap matrix (ESLint, Roslyn/StyleCop, first-party SAST, coverage dashboard, duplication, cognitive-complexity trend, multi-tool consolidation, quality gates). Document what Codacy's open-source tier actually bundles, GitHub permission scope, and whether residual value exists after SonarQube Cloud + CodeQL are in place. Recommend adopt / skip with reconsider-triggers. | DEC-034 parked Codacy as "optional Phase 2 SAST gate." Phase 2 never arrived because the private-repo amendment froze the pipeline shape. Need to decide explicitly rather than leave it implicit, and feed the result into ROADMAP — either as a restoration item or an explicit deferral with reconsider-triggers. | Done | batch-14e-codacy-reevaluation.md |
| R-041 | Branch protection / rulesets for a solo-dev public OSS repo | Produce a click-by-click ruleset configuration for `main` plus a phased rollout plan. Decide legacy branch protection vs rulesets (2026 best practice), required-status-check sequencing during tool onboarding (avoid blocking merges before tool baselines exist), emergency-bypass procedure with audit trail, signed-commits / merge-method interaction, linear-history strategy, and Dependabot interaction. | DEC-034's private-repo amendment deferred branch protection because it required GitHub Pro on private repos. Free on public. This is the durability mechanism for every other tool in the restoration — the required-check wiring is what turns each scan from advisory to load-bearing. Soft-launch sequencing is the key risk (don't block DEC-042 implementation PRs on checks that haven't produced a baseline yet). Feeds ROADMAP Next Up §3. | Done | batch-14f-branch-protection-ruleset.md |
| R-042 | License, trademark, and attribution strategy for an OSS AI coaching product | Produce a license selection (compare MIT / Apache-2.0 / MPL-2.0 / AGPL-3.0 / BUSL-1.1 / PolyForm NC for coaching-prompt IP protection + community contribution goals), Daniels/VDOT trademark strategy grounded in real enforcement precedent (Runalyze, The Run SMART Project LLC), `NOTICE` / `THIRD-PARTY-NOTICES.md` template for bundled OSS, research-artifact provenance convention for `docs/research/artifacts/`, coaching-prompt dual-license question (prompts as content vs code), and README structure. | The repo went public on 2026-04-15. `package.json` declares `license: ISC` but there is no `LICENSE` file, which under Berne defaults effectively to "all rights reserved" — a concrete legal gap. DEC-042 planned a narrow Daniels trademark disclaimer but the public flip requires a broader pass. This is item 8 in ROADMAP Next Up §3 and cannot be deferred. | Done | batch-14g-license-trademark-attribution.md |
| R-043 | License-compliance CI workflow for a .NET 10 + React 19 monorepo | Produce two ready-to-commit workflows: `license-review.yml` (PR-triggered dependency-review-action with license allow/denylist keyed to the chosen repo license) and `license-sbom.yml` (scheduled weekly SBOM). Compare `actions/dependency-review-action`, `anchore/sbom-action` (Syft), `fossas/fossa-action`, and `trufflesecurity/trufflehog`. Resolve the .NET Central Package Management gap (GitHub's static dependency graph doesn't parse `Directory.Packages.props`), handle npm `package-lock.json` v3, produce denylists for MIT / Apache-2.0 / AGPL-3.0 as possible repo licenses, and document the exception-handling workflow. | ROADMAP flagged this as "add pre-public release." The public flip is the trigger. Without it, a transitive dependency on a GPL/AGPL package could silently force the project to copyleft. Fails closed on PR, SBOM on schedule. Feeds ROADMAP Next Up §3 item 9; depends on R-042 for the chosen repo license. | Done | batch-14h-license-compliance-workflow.md |

### Batch 5 (Development infrastructure — feeds repo setup and CI/CD)

- **Batch 5:** R-012 (AI PR review and code quality tooling) — needed before repo scaffolding so the quality pipeline is designed into the project from the start.

### Batch 6 (LLM testing architecture — feeds POC 1 eval refactor)

- **Batch 6:** R-013 + R-014 (LLM eval strategies and .NET tooling) — directly informs POC 1 eval suite refactor. R-013 covers eval strategies and assertion patterns. R-014 covers .NET library choices. Together they feed DEC-036.

### Batch 7 (Implementation discoveries — feeds POC 1 eval debugging and model strategy)

- **Batch 7:** R-015 + R-016 (IChatClient bridge gap and model ID versioning) — discovered during POC 1 eval implementation. R-015 identified that the Anthropic SDK's IChatClient bridge silently drops structured output schemas. R-016 resolved model ID confusion and established versioning strategy. Together they feed DEC-037 and DEC-038.

### Batch 8 (CI infrastructure — feeds PR #18 review fixes)

- **Batch 8:** R-017 (eval cache TTL strategy) — discovered during PR #18 code review. M.E.AI's 14-day TTL breaks committed CI fixtures. Research identified four workarounds; DEC-039 chose post-process approach.

### Batch 9 (POC 1 review findings — feeds PR #17 fixes and unit architecture)

- **Batch 9:** R-019 + R-020 (Daniels data verification and unit system design). R-019 is blocking PR #17 merge — pace table data must be verified. R-020 is forward-looking — informs how to build unit handling cleanly before MVP-1 introduces imperial users. R-019 feeds immediate code fixes. R-020 feeds a future DEC on unit architecture.

### Batch 10 (REVIEW.md configuration — feeds AI code review setup)

- **Batch 10:** R-021 + R-022 + R-023 + R-024 (frontend practices, backend practices, CI/quality gates, REVIEW.md configuration). All four feed the generation of hierarchical REVIEW.md files (root + backend/ + frontend/). R-021 also validates frontend library versions. R-023 addresses private repo constraints. R-024 provides the meta-framework for writing effective review rules. Run all four in parallel — no dependencies between them.

### Batch 11 (Durable pace-zone implementation — DEC-040 follow-up)

- **Batch 11:** R-025 (Daniels pace-zone implementation pattern and library survey). Follow-up to R-019 after a 2026-04-14 computational audit exposed residual anomalies at VDOT 49→50 in the Interval and Repetition columns that the DEC-040 row-shift patch did not resolve. R-025 established the pure-equation direction (Pattern 1) with committed fixture as test harness (Pattern 3), but contains two known inaccuracies around I-pace and R-pace derivation (see Errata).

### Batch 12 (Deep verification — DEC-042 foundation)

- **Batch 12:** R-026 through R-031 plus R-034 — comprehensive disambiguation pass after R-025 landed with partial answers. R-026 pivoted from "4th edition table capture" (refused as copyright-sensitive) to "complete equation reference"; R-027 verified coefficient stability; R-028 pinned T = 88.0% and I = 97.3% as fixed constants; R-029 confirmed 5 zones (+ optional F) and continuous VDOT semantics; R-030 verified `VdotCalculator` equations correct (but found 5 missing race distances and missing input guards); R-031 established Tanaka as the max-HR replacement and HR zones as a separate calculator; R-034 confirmed equation-derived fixtures eliminate legal risk and the current book-transcribed lookup table must be retired before any public release. R-032 and R-033 were intentionally deferred as non-blocking for DEC-042 correctness. Together, the batch-12 artifacts ground DEC-042's design.

### Batch 13 (R-pace disambiguation — final gap before implementation)

- **Batch 13:** R-035 (R-pace mile-vs-3K formulation disambiguation). Narrow, single-purpose research to pick between R-025's mile-race-prediction and R-028's 3K-with-multipliers formulations for Repetition pace. Uses vdoto2.com as oracle and GoldenCheetah's `VDOTCalculator.cpp` as reference implementation. **Result: Option B (3K × multipliers) wins with max |error| ≤ 1.1 s and RMS 0.53 s across VDOT 30–85. Option A shows a −1.2 s fast bias at VDOT 55–65. GoldenCheetah's 105%-vVDOT approach drifts ≥ 2 s at the tails.** Bonus findings: F-pace = predicted 800 m race time ÷ 2 (the "F = R − 3 s/200 m" rule is wrong); R-800 ≈ 2 × R-400 simplifies the Option B 0.9528 multiplier without accuracy loss. DEC-042 is now fully specified and ready for implementation.

### Batch 14 (OSS Quality Tooling Restoration — triggered by 2026-04-15 public repo flip)

- **Batch 14:** R-036 through R-043 — eight-artifact research pass to restore the quality-pipeline tools that DEC-034's 2026-03-19 private-repo amendment had stripped out, and to close the public-repo hygiene gaps (LICENSE, attribution, trademark, license-compliance CI) that the public flip newly exposed. Three tools confirmed for restoration: **CodeRabbit** (R-036 — free-forever Pro tier for public repos, GitHub App install with zero CI changes, schema v2 still current), **CodeQL** (R-037 — v4 is now the current major since October 2025, `.slnx` autobuild support added in CodeQL 2.24.0 Jan 2026 but `build-mode: manual` still recommended, combined `javascript-typescript` identifier), and **SonarQube Cloud** (R-038 — product officially renamed from SonarCloud in October 2024, unified `sonarqube-scan-action@v7` with embedded Java 21, monorepo requires two separate projects, **Cobertura reports will NOT work for .NET — must add OpenCover output from Coverlet**, built-in MCP server integration for Claude Code user-side). Two tools re-evaluated: **Snyk** (R-039 — free tier is unlimited for public repos; unique value is `@snyk/protect` transitive-patching for npm only and proprietary-DB ~47 days faster CVE discovery; recommended as **layered on top** of the existing stack rather than substitute) and **Codacy** (R-040 — **no residual value** confirmed; literally ships the same SonarAnalyzer.CSharp NuGet and the same ESLint + eslint-plugin-sonarjs bundle the project already runs, SonarQube Cloud replicates its dashboard with deeper metrics; explicit skip recommendation with reconsider-triggers). **Branch protection** (R-041 — use rulesets, not legacy branch protection; "signed commits" and "rebase merge" are mutually incompatible in the web UI so squash-only; phased rollout plan for required-check sequencing). **License strategy** (R-042 — strong recommendation: **Apache-2.0 for code + CC-BY-NC-SA-4.0 for `backend/**/Prompts/*.yaml`**, with a concrete **VDOT trademark avoidance requirement** grounded in actual enforcement precedent — The Run SMART Project LLC compelled Runalyze to remove all VDOT features, and a newly public repo will not fly under the radar; the current `license: ISC` in `package.json` with no `LICENSE` file is a Berne-default "all rights reserved" legal gap). **License-compliance CI** (R-043 — `actions/dependency-review-action` v4.9.0 as PR gate + `anchore/sbom-action` v0.22.0 for weekly SBOM; **critical blocker: GitHub's static dependency graph does NOT parse `Directory.Packages.props`**, so .NET CPM projects must enable GitHub's Automatic Dependency Submission for NuGet which launched July 2025). All eight artifacts feed the OSS Quality Tooling Restoration item in ROADMAP Next Up §3 and the research plan at `docs/specs/research-oss-tooling-restoration/research-oss-tooling-restoration.md`.

---

## Errata

Concise index of known inaccuracies in prior research artifacts. Each affected artifact also carries an inline errata note at the top of its file.

- **R-019 (`batch-9a-daniels-pace-table-verification.md`)** — Did not verify VDOT 30–49 cell values; "likely correct" conclusion was based on step-size consistency alone. A 2026-04-14 audit found residual VDOT 49→50 anomalies in Interval (+1.55 pp) and Repetition (+3.69 pp) after the DEC-040 row-shift fix. Superseded by the equation-based direction in DEC-042.
- **R-025 (`batch-11-daniels-implementation-patterns.md`)** — Two claims corrected: (1) "I-pace = 100% VO₂max" — actual fixed constant is 97.3%, per R-028; (2) "R pace = predicted mile race pace" — **rejected by R-035** in favor of R-028's 3K-with-multipliers formulation (Option A showed a −1.2 s fast bias at VDOT 55–65). Pattern 1 / Pattern 3 recommendation is still valid.
- **R-028 (`batch-12c-pace-zone-derivation-constants.md`)** — R-pace derivation via 3K race prediction × distance-specific multipliers (0.9295 / 0.9450 / 0.9528) was attributed to a LetsRun reverse-engineering rather than first-party source. **R-035 confirmed it as the authoritative formulation**, with max |error| ≤ 1.1 s across VDOT 30–85 vs. the published Daniels tables. The 0.9528 R-800 multiplier slightly overshoots and is simplified in DEC-042 to `R-800 = 2 × R-400` without accuracy loss. T = 88.0% and I = 97.3% constants are unaffected and remain authoritative.

### Status Key

- **Queued** — Ready to hand off
- **In Progress** — Research agent working on it
- **Done** — Artifact complete, needs integration
- **Integrated** — Takeaways pulled into planning docs
