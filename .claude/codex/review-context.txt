<task>
{lens_task}
</task>
<context>
You are reviewing the {slice} build of the RunCoach repo in a detached git worktree (your cwd). Read AGENTS.md first; it holds the sandbox facts and the house rules. The build is committed on this worktree's HEAD; `git diff {diff_base} --stat` and `git diff {diff_base}` show exactly what the implementer changed. Read next: {spec_path} (the spec: BDD acceptance criteria and the decisions it cites), the CLAUDE.md files that cover the touched tree, REVIEW.md (root and backend/ or frontend/ as relevant). The implementer's stage report is at .stage-report.md: read it, verify every claim, trust none.
Allowed commands beyond reading: {allowed_commands}. Never write outside this worktree except under /tmp; git is read-only for you (log, diff, show, status). Every command runs in a fresh shell: create `/tmp/codex-agent-<lens>` once and set `TMPDIR=/tmp/codex-agent-<lens>` inline on every command that needs it. Restore any file you mutate from `git show HEAD:<path>` and finish with `git status --short` showing only untracked scratch files.
</context>
<severity_scale>
blocker: an acceptance criterion is not met; a build, lint, or type gate the implementer claimed green is red; a secret, a migration edit, a hand-edited generated file, or a user-facing trademark term entered the diff; a persisted event or wire contract changed without the delta the spec names.
major: a test that cannot fail against the bug it names; a silent skip or swallowed exception; a claim in the stage report the code does not support; a spec decision silently deviated from; a codegen or eval-cache pairing left stale; an accessibility or contrast rule the frontend CLAUDE.md pins broken.
minor: naming, comments, ordering, a missing citation, style a lint rule does not already catch.
</severity_scale>
<grounding_rules>
Every finding cites file:line in THIS worktree and, for a runtime claim, the command and its output line. Mark each finding MECHANIZED (you ran it) or INSPECTED (you read it). Never report a red you did not run. Judge the diff against the spec and the house rules; do not propose a redesign. ASCII only; every string under 600 characters.
</grounding_rules>
<structured_output_contract>
Return ONLY JSON matching the schema you were given: {{gate, findings[{{severity, claim, evidence, fix, mode}}], mutations[{{mutation, expected_red_test, observed}}], conformance[{{item, status, where}}], checks_run[{{command, result}}], orchestrator_runs[{{command, purpose}}], verdict}}. gate is ACCEPT only with zero blocker and zero major. A mutation or check may be marked NOT_RUNNABLE_IN_SANDBOX only after you attempted the command and pasted the sandbox's own error text as its evidence; then put the exact command into orchestrator_runs. The orchestrator runs every orchestrator_runs entry and attaches the output to the round record; until then that mutation counts as unverified, never as green, and the gate stays REJECT if it guards a blocker or major.
</structured_output_contract>
