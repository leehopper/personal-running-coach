{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "[ -n \"$TMPDIR\" ] && mkdir -p \"$TMPDIR\" 2>/dev/null; exit 0"
          }
        ]
      }
    ]
  }
}
